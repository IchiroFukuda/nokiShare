# データベースセットアップ

## 概要
このプロジェクトでは、Supabaseを使用したデータベースセットアップを提供しています。

## ファイル構成
- `database-setup-complete.sql` - 完全なデータベースセットアップファイル（推奨）

## セットアップ手順

### 1. Supabaseプロジェクトの作成
1. [Supabase](https://supabase.com)にアクセス
2. 新しいプロジェクトを作成
3. プロジェクトの設定からデータベース接続情報を取得

### 2. データベースの初期化
1. Supabaseダッシュボードの「SQL Editor」を開く
2. `database-setup-complete.sql`ファイルの内容をコピー
3. SQLエディタに貼り付けて実行

### 3. 実行される内容
このSQLファイルを実行すると、以下の要素が自動的に作成されます：

#### テーブル
- `companies` - 会社情報
- `users` - ユーザー情報
- `email_verification_tokens` - メール確認トークン
- `products` - 製品情報
- `chat_messages` - チャットメッセージ
- `chat_audit_log` - チャット監査ログ

#### セキュリティ
- Row Level Security (RLS) の有効化
- 適切なセキュリティポリシーの設定
- ユーザー権限の設定

#### パフォーマンス
- 必要なインデックスの作成
- トリガー関数の設定
- 自動更新日時の管理

## 注意事項

### 既存データベースへの適用
- 既存のテーブルがある場合は、`IF NOT EXISTS`により安全に処理されます
- 既存のデータは保持されます

### セキュリティ
- 本番環境では、適切な環境変数とシークレット管理を行ってください
- RLSポリシーは基本的な設定のみ含まれています

### カスタマイズ
- 必要に応じて、テーブル構造やポリシーをカスタマイズしてください
- ビジネスロジックに合わせてセキュリティポリシーを調整してください

## トラブルシューティング

### よくある問題
1. **権限エラー**: Supabaseの認証設定を確認
2. **テーブルが作成されない**: SQLの構文エラーがないか確認
3. **ポリシーが適用されない**: RLSが有効になっているか確認

### ログの確認
- Supabaseダッシュボードの「Logs」セクションでエラーログを確認
- SQLエディタの実行結果でエラーメッセージを確認

## サポート
問題が発生した場合は、以下を確認してください：
1. Supabaseのドキュメント
2. プロジェクトの設定
3. SQLファイルの構文

---

**重要**: 本番環境に適用する前に、必ずテスト環境で動作確認を行ってください。 
